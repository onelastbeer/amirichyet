import * as types from '../mutation-types'

// initial state
const state = {
  currencies: ['ETH', 'BTC', 'EOS'],
}

// getters
const getters = {
  currencies: state => state.currencies
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
